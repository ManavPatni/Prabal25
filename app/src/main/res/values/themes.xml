<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Prabal25" parent="Theme.Material3.Dark.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/primaryBg</item>

    </style>

    <!--Bottom Nav-->
    <style name="CustomBottomNavStyle" parent="Widget.MaterialComponents.BottomNavigationView">
        <item name="itemRippleColor">@android:color/transparent</item>
        <item name="itemTextColor">@color/bottom_nav_item_color</item>
        <item name="itemIconTint">@drawable/bottom_navigation_color_selector</item>
        <item name="android:background">@drawable/custom_bottom_nav</item>

        <item name="itemIconSize">20dp</item>
        <item name="itemPaddingTop">8dp</item>
        <item name="android:elevation">5dp</item>
    </style>

</resources>